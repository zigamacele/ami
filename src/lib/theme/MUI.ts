import { createTheme } from '@mui/material';

export const inputsTheme = createTheme({
  palette: {
    primary: {
      main: '#BCBCBC',
    },

    background: {
      paper: '#171717',
    },
    text: {
      primary: '#BCBCBC',
      secondary: '#929292',
    },
    action: {
      active: '#929292',
    },
  },
});
